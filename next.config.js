/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  devIndicators: {
    buildActivity: false, // Yükleme göstergesini kapatır
  },
};

module.exports = nextConfig;
